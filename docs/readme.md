# Event App

Mobile application for organizing events.

## Features

- Create events
- Manage participants
* Deploy to Azure   <-- ДОДАНО

## Git Process Notes

This section will be updated during the lab to document the Git process used in VS Code (GUI).

* **Repository Structure:**
    * `src/`: Contains source code (HTML, CSS, JS).
    * `docs/`: Contains documentation (like this README).

* **Scalability Example:** Parallel development via branches (e.g., `feature-style`, `feature-docs`).
* **Resilience Example:** Ability to revert committed changes.